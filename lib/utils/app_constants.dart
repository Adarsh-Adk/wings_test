import 'package:flutter/material.dart';

class AppConstants {
  static const appName = "Wingman";
  static const cornerRadius = 10.0;
  static const defaultPadding = 16.0;
  static const defaultEdgeInsets = EdgeInsets.all(defaultPadding);
  static const elevation = 0.0;
  static final borderRadius = BorderRadius.circular(AppConstants.cornerRadius);
  static const baseUrl = "https://test-otp-api.7474224.xyz/";
}
